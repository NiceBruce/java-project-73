server:
  port: ${PORT:4896}

base-url: /api

spring:
  application:
    name: app

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml

  mvc:
    hiddenmethod:
      filter:
        enabled: true

#
#  datasource:
#    username: "sa"
#    password: ""
#    url: "jdbc:h2:./task_manager"

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

